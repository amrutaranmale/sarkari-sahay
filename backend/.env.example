# SQLite (default — works without installing PostgreSQL)
DATABASE_URL="file:./dev.db"

# PostgreSQL (production) — uncomment and use schema.postgresql.prisma approach or change provider in schema.prisma
# DATABASE_URL="postgresql://postgres:password@localhost:5432/sarkari_sahay?schema=public"

PORT=3001
